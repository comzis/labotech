API_HOST=10.67.18.29
API_PORT=4000

# ── CPU / Memory ─────────────────────────────────────────────────────────────
# 2× Xeon Gold 5120: 28 physical cores, 56 logical (HT).
# NUMA node 0: physical 0-13 + HT siblings 28-41  →  labotech
# NUMA node 1: physical 14-27 + HT siblings 42-55  →  encapsulator
# Each container pinned to its own NUMA node avoids cross-socket memory
# latency for child processes (ffprobe, tsanalyze, ffmpeg).
LABOTECH_CPUS=24.0
LABOTECH_CPUSET=0-13,28-41
LABOTECH_MEM_LIMIT=24g
LABOTECH_SHM_SIZE=1g

ENCAPSULATOR_CPUS=16.0
ENCAPSULATOR_CPUSET=14-27,42-55
ENCAPSULATOR_MEM_LIMIT=8g
ENCAPSULATOR_SHM_SIZE=512m
ENCAPSULATOR_HOST=127.0.0.1
ENCAPSULATOR_PORT=4100

# ── Node.js heap ─────────────────────────────────────────────────────────────
# mem_limit is 24 GB — leave ~4 GB headroom for child process overhead.
NODE_OPTIONS=--max-old-space-size=20480

# ── TS probe concurrency ─────────────────────────────────────────────────────
# Each heavy probe spawns ffprobe + tsanalyze — mostly NIC I/O wait, not CPU.
# 8 simultaneous heavy probes is safe on 28 logical threads.
TS_HEAVY_PROBE_MAX_CONCURRENT=8
# Thumbnail captures: 4 concurrent (was 2).
THUMBNAIL_MAX_CONCURRENT=4

# ── Encapsulator guardrails ───────────────────────────────────────────────────
ENCAP_CPU_GUARDRAIL_ENABLED=true
ENCAP_CPU_WARN_PCT=70
ENCAP_CPU_BLOCK_PCT=75
ENCAP_CAPACITY_PER_CORE=20
ENCAP_CAPACITY_STREAM_MBPS=22

# ── TSDuck / TS input tuning ─────────────────────────────────────────────────
TS_INPUT_FIFO_SIZE=10000000
TS_INPUT_TIMEOUT_US=7000000
TS_INPUT_REORDER_QUEUE_SIZE=512
