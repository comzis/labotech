# Cursor Project Setup

This repository now includes:

- `.cursor/rules/` for always-on and file-scoped coding rules.
- `.cursor/memory/project-memory.md` for persistent project context.
- `.cursor/skills/` for reusable review and release workflows.
- `.cursor/tools/standard-toolbox.md` for standard run/test/build commands.
